events {}

http {
    include /etc/nginx/mime.types;

    server {
        listen 80;
        root /usr/share/nginx/html;

        location / {
            try_files $uri $uri/ /index.html;
        }

        location /store/ {
            proxy_pass http://backend-springboot:8080/store/;
        }
    }
}
